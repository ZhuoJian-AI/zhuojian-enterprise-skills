# 资料识别与对象归属规则（本地候选）

2026-09-22，Codex，BUSINESS-IDENTITY-REVIEW-20260922；规则版本 43dff14。基于既有 draft PR #35 工作树 49a761a，任务认领 ab32dc6。未更改已安装 Skill、版本元数据、2.4/2.5 Manifest、Runtime 或业务系统。

## 修改与责任

- 通用入口和业务交付参考路由到 `references/business-identity-review.md`；按文件识别、批量导入和对象关联适用，不强制所有页面变成 AI 页面。
- 来源可追溯，按已确认业务规则解释主对象、参考/旧模板和文件名；不按首候选、目录顺序或已有主档默认归属。不强求多个来源，单一权威字段可构成充分依据。
- 正常项集中处理，员工只核对歧义；分类确认不能代替归属确认。目标/文件/版本变化重新预检，页面与 Action 共用后端保护，浏览器 reviewed=true 不是放行依据。
- 逐项回执、幂等和未知写入核实；取证由获授权子系统维护方承担，不要求平台负责人代员工寻找原件。
- 复用现有 `docs/ai-delivery.json`，没有新增线上状态机或结果协议。SaaS 的模型提示补强不能替代子系统校验，也不能确定性证明一个任意 Action 内全部项目的结果。

## 验证

在 `skills/core/zhuojian-subsystem-builder`：

```text
python -m pytest tests/test_ai_delivery.py -q
python C:/Users/王鑫涛/.codex/skills/.system/skill-creator/scripts/quick_validate.py .
```

30 passed；Skill is valid。对本轮三个 Markdown 文件的相对链接解析全部通过；`git diff --check` 通过。未改运行脚本或模板，未机械重复全套测试。

独立 agent 在不知原案例结论的离线请求中，以“60 份订单 Excel，文件名/内容冲突、旧 sheet、平台负责人无原件”前向演练。输出正常项批量处理、异常核对、按权限取证、不把缺原件当全部开发阻塞；指出原文“多个来源一致才是依据”可能增加无谓工作，已修正为按业务规则判断充分性。演练不是实际员工、实际文件或服务器验收。

## 同步与发布

本轮仅本地修改和检查，未推送本轮增量、未合并、未发稳定 Release。原 draft PR #35 和配套 SaaS #358 不是稳定版。安装版仍为 core 1.1.18，不能让业务负责人误以为再次调用已取得本轮规范。

新增统一助手入口依次发布 SaaS 前后端、稳定 Skill，再由业务负责人安排自己的开发 AI 适配、登记与验收。生产协同已复现的匹配/归档代码缺陷可以独立先修，不以公共能力发布为前置。当前真实服务器版本、原文件内容及是否已经错归档仍待其维护方核实。

当前没有修改任何现有子系统。生产协同须处理实际匹配/归档；企业文化、商品动销仅在存在同类导入/对象关联流程时按范围检查，不一律复制款号规则。发布后按组织规则通知下游及更新 wiki，本轮未将未发布候选标成稳定契约。
