# 业务提效 Skill 交付升级

任务：BUSINESS-AI-DELIVERY-20260922；执行：Codex。源码基线 `51fcab5`，部署规则 `43dff14`。发布状态以本文件后续证据及稳定 Release 为准，不以候选版本号推断。

## 责任与范围

- 主责 Skill：总入口、随包模块需求、开工模板、业务能力映射和本地交付校验。
- SaaS：本轮不改运行代码、不部署。单一助手、供应商管理、编排、有效授权、确认和公共 UI 保持平台职责。
- 子系统：本轮不改业务仓库、Runtime、Manifest、业务数据或员工角色，不部署企业 ECS。现有流程保持运行；下一次新增/实质改造业务时应用新交付要求，小范围维护不强制全量迁移。
- `contractRevision=2.4/2.5` 不变；`docs/ai-delivery.json` 是项目内证据索引，不是平台接口、Manifest 扩展或可执行调度。

## 实施内容

开发 AI 自动从工作链识别提效机会，负责人只确认业务事实和重要风险。查询/分析/草稿/确认后执行成为可提出的完整方案，修正附属材料中默认只查不能改、用留痕代替权限、无数据即无异常、最后才补 AI 的冲突。人工-only、无收益、普通确定性自动化仍合法，不强制每个页面接入模型。

新增 `references/business-ai-delivery.md`、合成设计示例与只读 `validate_ai_delivery.py`。平台部署、企业启用、员工授权和真实业务验收分别留证；未知和部分成功不能报告为完成。校验器不联网、不写业务、不证明引用证据为真；版本/权限/副作用仍由既有运行时负责。

## SaaS 后续能力核对与需求移交（不是本轮已实现）

1. **目标环境能力核对入口**：开发 AI 需要不含管理秘密的已部署能力/契约证据；先复用现有受控目录。是否需要新增统一发现接口待平台核对，不在 Skill 发明端点或 CLI。
2. **主动业务触发与委托**：在本轮前审查的 SaaS 源码 `9b1e6f1` 中，事件投递与运行恢复不能作为自动创建业务 AgentRun 的证据。若需求是定时/事件主动运行，应另行核对当前部署并提供明确主体、有限范围、到期/撤销、预算、去重、实时核权、确认与审计。此处是需求，不是断言所有目标环境永不支持。
3. **跨层缺口回执**：开发 AI 应输出责任方、缺口、验收条件和当前替代方法；通过已有获准协作渠道交接。没有渠道只生成“待提交”资料，不声称已受理，不让业务负责人传话或提供平台 root。

上述需求应作为 SaaS 后续工作核对，不得借更新 Skill 启用后台操作。平台运行能力先发布、再发布真实 Skill 接口、最后由受影响子系统适配。

## 验证记录

- 核心目录 `python -m pytest -q -rs`：274 passed、41 skipped（23.95 秒）；其中 30 项为新增交付校验器测试。跳过项为 3 项 POSIX 权限检查、38 项须在生成项目运行的模板集成检查。
- 用既有 `scaffold_subsystem.py` 生成合成 `sample-review` 项目，在生成目录运行 `python -m pytest -q --rootdir . tests`：59 passed、1 skipped；既有 FastAPI on_event 弃用警告 12 条。第一次跨盘从仓库目录运行 pytest 导致收集根目录/模块路径错误，改为生成项目目录后通过，不计为业务故障。
- 合成 `business-ai-delivery.example.json` 对生成 Manifest 的设计检查通过；带 `--require-verified --journey review-completeness` 返回 exit 1，正确拒绝将 planned 宣称完成。
- 核心 `quick_validate.py` 通过。附属中文 `模块需求` 沿用原名字，不符合该通用脚本的英文 hyphen-case 命名限制；未改名以保持旧调用兼容，不将此项报告为通过。
- 独立、未给预期答案的 Skill 前向情景试跑：重复核对录入、后台催办、纯视觉小改、仅查不写、数据源不可用共 5 例；能区分可交付能力、待核实依赖和未经授权副作用。这是规则引导评估，不是在线模型或业务端到端验收。
- 二次审查修复：按本次 journey 选择严格验收；普通自动化优于 AI 不再被记作负责人拒绝；沿用全平台北京时间。独立复核相关测试 7 passed、23 deselected，无新增阻塞。
- 前向试跑发现的旧访谈矛盾已修正：证据充分不强制当面重访谈，设计样例与真实接入核验分开，仅阻塞受缺口影响的任务，统一一次只追问一个重要缺口。编辑代理另做 3 例只读情景复演通过，非独立盲测。
- 附属 AGENTS 连续重建一致，HTML 内 3 个 SVG 可解析，`git diff --check` 通过。

原仓库无云端 workflow；本轮补最小只读核心测试闸门，实际运行链接及稳定包、公开安装证据另行记录。任何本地/云端包测试都不替代真实子系统端到端验收。
