# 更新记录

## 1.1.0 - 2026-09-11

- 将通用搭建入口从历史名称 `aifabei-subsystem-builder` 切换为 `zhuojian-subsystem-builder`；道讯只读边界和服务器匹配范围不变。

## 1.0.0 - 2026-09-11

- 将已经过验证的 Alphabet 道讯 SQL Server 只读桥接交接规则纳入唯一企业 Skills 总仓库。
- 按企业、Runtime 或 ECS 主机匹配后由 `aifabei-subsystem-builder` 自动安装和更新，不再要求负责人单独领取压缩包。
- 保留反向隧道、只读数据库、Docker 私网、SaaS 子系统接入和分层验收边界。
