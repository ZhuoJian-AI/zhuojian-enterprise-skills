[Unit]
# Refresh the volatile storage gate before Docker restores managed apps.
# Wants (not Requires) ensures a monitor failure cannot block unrelated legacy
# containers; managed apps still fail closed if the state file is unavailable.
Wants=zhuojian-disk-monitor.service
After=zhuojian-disk-monitor.service
